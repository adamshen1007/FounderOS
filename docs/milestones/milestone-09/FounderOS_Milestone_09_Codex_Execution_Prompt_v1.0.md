You are the lead engineer responsible for implementing FounderOS Milestone 09 — Durable Snapshot Registry and Activation Audit Foundation. Begin only after confirming that Milestone 08 has been merged into the latest `main`, the current branch is dedicated to Milestone 09, and the worktree contains no unrelated changes. Read and follow `README.md`, `AGENTS.md`, `CONTRIBUTING.md`, `ARCHITECTURE_DECISIONS.md`, `DOCUMENTATION_INDEX.md`, `docs/reviews/REPOSITORY_AUDIT_v1.0.md`, every specification under `docs/milestones/milestone-04/` through `docs/milestones/milestone-09/`, and the current implementations under `packages/knowledge-schema/` and `services/knowledge-engine/`. Understand the Milestone 07 snapshot contracts and identity model, the Milestone 08 lifecycle, comparison, evidence, review, approval, rejection, and activation APIs, all current public exports, tests, serialization rules, and dependency boundaries before modifying code. The objective is to make governed snapshot state durable, recoverable, auditable, and integrity-verifiable across process restarts without coupling shared KnowledgeOS contracts to a specific database or retrieval technology. Implement storage-independent runtime contracts and strict schemas for an immutable snapshot registry record, durable lifecycle transition record, durable approval or rejection decision evidence where needed, activation audit record, atomic activation transaction, registry recovery result, and integrity verification result. Shared contracts must remain owned by `@founderos/knowledge-schema`; they must reject unknown fields, preserve versioning, support deterministic canonical serialization, and must not expose SQL, filesystem-path, vendor-transaction, embedding, ranking, agent, or MCP concepts. Implement durable registry and audit-store behavior in `@founderos/knowledge-engine`, including immutable snapshot registration, deterministic snapshot listing and lookup, append-only lifecycle and decision history, activation history, current active-snapshot resolution, complete registry recovery, and integrity verification. Treat lifecycle history and activation evidence as immutable records rather than a mutable status column. Preserve Milestone 08 lifecycle and approval semantics. Implement activation as an all-or-nothing governed transaction: verify that the candidate snapshot is registered and approved, verify that the referenced approval decision and change set bind to the correct baseline and candidate snapshots, enforce `expectedActiveSnapshotId` compare-and-swap semantics, transition the previous active snapshot to `Superseded` when present, transition the approved candidate to `Active`, persist the activation audit evidence, advance the audit chain, and make the resulting active state recoverable. A failed precondition, stale expected-active value, invalid evidence reference, fingerprint mismatch, or interrupted uncommitted write must leave no committed partial state. Replaying the same transaction ID with the same canonical payload must be idempotent; reusing the same transaction ID with different content must fail. Implement recovery by loading only committed authoritative records, verifying canonical payload fingerprints, verifying audit-chain continuity, replaying lifecycle and activation records in explicit durable order, and checking invariants including unique snapshot identities, valid transitions, valid evidence references, no contradictory terminal states, consistent supersession history, and at most one active snapshot. Recovery must fail closed on corruption, missing records, broken audit links, contradictory activations, or partial records presented as committed. Do not silently skip or repair authoritative history. Implement one minimal replaceable local file-backed adapter to prove the contracts and governance semantics without introducing a general-purpose application database. Use an explicit configurable runtime root isolated from canonical `docs/` and `knowledge/` sources, preferably under a Git-ignored path such as `.founderos/runtime/knowledge-registry/`. Authoritative committed records must be immutable and append-only. Derived indexes or active-snapshot caches may exist only if they are rebuildable and non-authoritative. Use canonical serialization, explicit single-writer protection, temporary writes in the target filesystem, and atomic commit semantics such as same-filesystem rename. Reject path traversal, symlink escape, writes outside the runtime root, and overwrite of unrelated repository files. Machine-specific absolute paths must not affect canonical record fingerprints. Scope distributed writers, remote coordination, databases, object stores, background synchronization, watchers, event streaming, embeddings, vector databases, semantic retrieval, ranking, knowledge graph persistence, agents, Hermes, MCP, integrations, and UI completely out of this milestone. Do not add a new framework or dependency unless the existing platform cannot satisfy the required durability and atomicity semantics; if a dependency is genuinely required, document the alternatives and decision in an ADR before implementation. Add focused tests for strict contract validation, unknown-field rejection, deterministic serialization, immutable registration, duplicate and conflicting identity behavior, adapter restart recovery, lifecycle-history recovery, approval-history recovery, activation-history recovery, exact active-snapshot recovery, atomic activation, baseline supersession, compare-and-swap failure, idempotent transaction replay, conflicting transaction reuse, crash simulation before and after commit, temporary-file exclusion, audit-chain verification, fingerprint tampering, missing evidence, contradictory activation history, duplicate active-state prevention, derived-index rebuild, path traversal, symlink escape, source immutability, and protection against arbitrary file overwrite. Preserve all Milestone 04 migration, Milestone 05 query, Milestone 06 repository, Milestone 07 corpus snapshot, and Milestone 08 lifecycle governance behavior and tests. Update only documentation that reflects implemented behavior, including the relevant package READMEs, root README, `DOCUMENTATION_INDEX.md`, `CHANGELOG.md`, and `ARCHITECTURE_DECISIONS.md`; record the durable registry and activation-audit decision in the ADR ledger or current ADR structure. Follow documentation-first development, architecture-before-code, strict TypeScript, existing package boundaries, small verified changes, no reverse dependency from schema to engine, no unrelated refactoring, and no unsupported completion claims. Before completion run `pnpm format:check`, `pnpm lint`, `pnpm build`, `pnpm typecheck`, `pnpm test`, and `git diff --check`. All checks and all prior milestone regressions must pass. Perform a final self-review focused on data corruption, partial commit exposure, lifecycle bypass, duplicate active snapshots, stale activation races, audit-chain integrity, path safety, and restart recovery. Return a final Milestone 09 report containing: status `GO` or `NOT READY`; branch and commit readiness; implementation summary; all added and modified files; tests added and total tests passing; exact verification results; durability and atomicity evidence; architecture impact and dependency direction; known limitations and deliberately deferred capabilities; any storage or crash-consistency assumptions; and the recommended next milestone. The milestone is `GO` only if FounderOS can register governed snapshots durably, record immutable lifecycle and activation evidence, restart, independently verify the complete audit history, and recover exactly one valid active knowledge state without relying on process memory. Prioritize governance integrity, crash safety, and recoverability over feature breadth.
