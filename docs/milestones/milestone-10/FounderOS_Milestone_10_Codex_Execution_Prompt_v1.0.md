You are the lead engineer responsible for implementing FounderOS Milestone 10 — Governed Knowledge Context Assembly Foundation. Your task is to implement the milestone completely, verify it, prepare it for review, and return a formal completion report. Do not stop after analysis or planning and do not claim completion unless every required verification gate passes. Before changing any file, fetch the latest remote state, confirm that Milestone 09 has been merged into the latest `main`, confirm that the current work is based on that merged state, and create or switch to a dedicated branch named `codex/milestone-10` if an equivalent branch does not already exist. Run and inspect `git status`, `git branch --show-current`, `git log --oneline --decorate -15`, and the relevant ancestry or merge-base checks. Preserve legitimate existing work and do not reset, discard, overwrite, or rewrite unrelated changes. If unrelated changes exist, report and isolate them safely. Do not include `.DS_Store`, iCloud conflict copies, temporary files, local registry data, generated context packages, locks, or test artifacts in the final commit. Read and follow `README.md`, `AGENTS.md`, `CONTRIBUTING.md`, `ARCHITECTURE_DECISIONS.md`, `DOCUMENTATION_INDEX.md`, `CHANGELOG.md`, `docs/reviews/REPOSITORY_AUDIT_v1.0.md`, every approved specification under `docs/milestones/milestone-04/` through `docs/milestones/milestone-10/`, and the current implementations under `packages/knowledge-schema/` and `services/knowledge-engine/`. Treat the Milestone 10 specification set as the implementation authority. Understand all existing public exports, strict schemas, canonical serialization and SHA-256 fingerprint logic, Milestone 04 migration and provenance authority, Milestone 05 query request and result behavior, Milestone 06 repository and candidate-source boundaries, Milestone 07 corpus snapshot identity, Milestone 08 lifecycle, comparison, review, approval, rejection, and activation semantics, and Milestone 09 durable registry, audit chain, atomic activation, recovery, integrity verification, and active-snapshot resolution. Do not create competing query, repository, snapshot, lifecycle, registry, evidence, or fingerprint systems. Preserve backward compatibility and the dependency direction `knowledge-engine -> knowledge-schema` unless the approved Milestone 10 specifications explicitly require a compatible versioned extension. The milestone objective is to create a governed Knowledge Context Assembly layer that transforms deterministic Knowledge Query results from the durably verified active Knowledge Snapshot into immutable, budget-bounded, provenance-complete, independently verifiable context packages. This milestone must not invoke an LLM, create model-specific prompts, execute an agent, perform semantic ranking, or make hidden relevance judgments. The completed system must prove exactly which active snapshot was used, which request and query were executed, which objects were included, which candidates were excluded or omitted, whether content was truncated, how budgets were calculated, and whether the final package can be reproduced byte-for-byte from the same governed inputs. Keep shared contracts storage-independent, model-independent, tokenizer-independent, and agent-independent. `@founderos/knowledge-schema` owns strict versioned request, package, budget, evidence, reason-code, fingerprint, and verification-result contracts. `@founderos/knowledge-engine` owns durable active-snapshot resolution, registry integrity verification, repository interaction, query execution, deterministic selection, budget enforcement, truncation, evidence generation, context fingerprinting, and independent verification. Do not expose SQL concepts, physical filesystem paths, model names, tokenizers, prompt roles, agent types, or MCP concepts in the shared context contracts. Implement a strict versioned `KnowledgeContextRequest` contract containing a stable request ID, explicit purpose, consumer context, the existing Knowledge Query contract, optional required object IDs, required object types, optional preferred object types, scope constraints, explicit assembly policy version, budget policy, request reason, and only justified caller evidence timestamps. Reuse existing human, service, and future-agent consumer context types where appropriate, but do not treat consumer context as authorization. Reject unknown fields, unsupported versions, invalid identifiers, empty purpose, duplicate required IDs, contradictory type or scope rules, unbounded or non-positive limits, contradictory budget settings, and unsupported policy versions. Implement a strict versioned budget policy supporting maximum object count, maximum canonical character count, optional per-object character limit, explicit truncation permission, required-object failure behavior, and empty-context behavior. Character count and object count are the authoritative Milestone 10 budget dimensions. Do not add a model-specific tokenizer dependency. If an optional token estimate is recorded, use one documented deterministic model-independent estimation method and mark it as evidence only. Implement a strict versioned `KnowledgeContextPackage` contract that binds to the request ID and canonical request fingerprint, purpose, consumer context, verified registry schema version, registry integrity fingerprint, active snapshot ID and content fingerprint, active manifest fingerprint, repository snapshot ID and fingerprint, query ID and canonical query fingerprint, query-result fingerprint, assembly policy version, budget policy, exact budget usage, ordered included object entries, excluded evidence, omitted evidence, truncation evidence, evidence counts, optional non-identity timestamp evidence, and a canonical content-derived context fingerprint. Reject unknown fields, invalid versions, incomplete bindings, contradictory evidence, invalid counts, invalid budget usage, or forged fingerprints. Return immutable values or defensive copies at asynchronous boundaries. Every included object entry must preserve object ID, object type, lifecycle status, relevant project or domain metadata, canonical included content, original object fingerprint, logical source identifier, source hash, provenance metadata, included-content fingerprint, included character count, selection position, and stable selection reason. Implement active-snapshot binding as a mandatory governed precondition. Use the Milestone 09 public application API to open or access the durable registry, independently verify registry integrity, recover the active snapshot, and resolve the matching repository snapshot. Fail closed before context output if recovery fails, integrity verification fails, no active snapshot exists when required, the repository snapshot does not match the durable active snapshot, content or manifest fingerprints differ, evidence is incomplete, or any binding is forged. Capture the resolved active-snapshot and registry-integrity binding once at the start of the assembly operation and use that immutable binding throughout; a later concurrent activation must not change an in-progress package. Reuse the Milestone 05 query contracts and Milestone 06 repository abstraction. The governed public context API must not read corpus files directly, bypass the repository, or accept arbitrary ungoverned candidates instead of the active repository snapshot. A lower-level pure helper may accept candidates only if it remains internal or clearly separated from the governed public API. Implement deterministic selection using explicit required IDs, required object types, preferred object types, project and domain scope, lifecycle status, tags, source, category, and existing exact filters. Do not add semantic ranking, text similarity, embeddings, LLM heuristics, or hidden relevance scoring. Define and version one deterministic ordering policy. Use this initial order unless repository constraints justify an equivalent documented policy: explicit required-object priority; request-declared required object-type order; request-declared preferred object-type order; documented governance or lifecycle priority; canonical object type; canonical project or domain identity; canonical object ID. Never rely on caller candidate order, filesystem enumeration, map iteration, wall-clock order, locale-sensitive sorting, or physical paths. Handle duplicates safely. Canonically equivalent duplicate candidates may be included once with deterministic duplicate evidence when appropriate. Conflicting candidates using the same object ID with different payload, fingerprint, or provenance must fail closed. Invalid candidates must never be silently ignored. Implement deterministic budget enforcement after required-object resolution and deterministic ordering. Enforce maximum object count, maximum canonical character count, and optional per-object character limits exactly. Define in code and documentation which canonical content representation is counted. Record requested limits, used object count, used character count, and per-object usage. Required objects must never be silently omitted. If required objects alone exceed a hard budget, return a stable failure or approved insufficient-context result and do not emit a misleading successful package. Empty context and insufficient context behavior must follow explicit request policy. Implement deterministic truncation only when explicitly enabled. Truncation must never mutate the original object. Record object ID, original character count, included character count, deterministic truncation boundary, stable reason code, original object fingerprint, included-content fingerprint, and provenance. Truncation must be Unicode safe and must not split surrogate pairs. Document whether the initial policy truncates raw canonical content or another explicitly defined field. Do not claim semantic completeness after truncation. Generate complete machine-readable evidence for included, filtered, duplicate, omitted, over-budget, truncated, invalid, and missing-required candidates. Exclusion evidence must name the applicable filter and stable reason code. Omission evidence must record policy rule, original deterministic ordering position, and budget impact where applicable. Invalid candidates should cause fail-closed assembly unless an approved rejection-result contract explicitly applies. Public errors and evidence must use logical source identifiers and must not expose usernames, checkout roots, absolute machine paths, runtime roots, or iCloud locations. Implement a canonical context fingerprint binding the contract version, canonical request payload, active snapshot and manifest fingerprints, registry integrity fingerprint, repository snapshot identity, canonical query and query-result fingerprint, assembly policy version, budget policy, ordered included entries, exclusion evidence, omission evidence, truncation evidence, and evidence counts. Wall-clock assembly timestamps may be recorded as non-identity evidence but must not alter context identity unless the request explicitly binds to a caller-supplied evidence timestamp. Identical verified registry state, active snapshot, repository snapshot, request, query behavior, and assembly policy must produce byte-identical canonical package serialization and the same context fingerprint, independent of candidate input order, filesystem location, runtime root, username, wall-clock time, and map iteration order. Implement a pure independent verification operation that recomputes the request fingerprint, query fingerprint where applicable, context fingerprint, included-content fingerprints, evidence counts, budget arithmetic, ordering, active snapshot binding, registry integrity binding, and repository snapshot binding. It must detect modified included content, changed provenance, forged object fingerprints, changed order, changed budgets, missing exclusion or omission evidence, invalid truncation evidence, incorrect counts, mismatched snapshots, and forged final fingerprints. Verification must fail closed and must not silently repair authoritative package data. Implement explicit successful-empty, insufficient-context, and over-budget behavior. A successful empty package is permitted only when the request policy explicitly allows it. Missing required knowledge must produce stable failure or an explicit governed insufficient-context result. Over-budget requests with truncation disabled must produce stable failure or an approved governed outcome, not an empty successful package. Add deterministic evaluation fixtures based on the approved Priority 1 FounderOS corpus and a durably active snapshot. Include governance context, architecture context, decision-focused context, empty matching set, missing required object, equivalent duplicate, conflicting duplicate, exact object-count boundary, exact character boundary, over-budget without truncation, over-budget with truncation, active snapshot mismatch, corrupted registry integrity evidence, forged repository binding, candidate-order permutation, included-content tampering, and omission-evidence tampering. Define expected included IDs, excluded IDs and reason codes, omission and truncation evidence, budget usage, and fingerprint behavior. Add comprehensive tests. Contract tests must cover valid and invalid requests and packages, unknown-field rejection, unsupported versions, contradictory policies, duplicate required IDs, invalid reason codes, canonical parsing, and forged fingerprints. Active-binding tests must cover valid durable active state, missing active state, corrupt registry, repository mismatch, content mismatch, manifest mismatch, incomplete evidence, and binding stability when a later activation occurs. Selection tests must cover every supported exact filter, required IDs, required and preferred types, deterministic ordering, candidate permutations, equivalent duplicate deduplication, conflicting duplicate rejection, empty results, and missing required knowledge. Budget tests must cover exact boundaries, required objects at and beyond boundaries, optional omissions, truncation disabled, truncation enabled, Unicode-safe deterministic truncation, per-object limits, and independent budget verification. Provenance and evidence tests must cover every evidence category and stable reason code. Reproducibility tests must prove byte-identical canonical output and identical fingerprints for identical governed inputs. Tampering tests must detect changed content, order, budgets, evidence, provenance, source hash, object fingerprint, active snapshot binding, registry integrity binding, repository binding, and final fingerprint. Preserve all Milestone 04 migration, Milestone 05 query, Milestone 06 repository, Milestone 07 corpus snapshot, Milestone 08 lifecycle governance, and Milestone 09 durability, recovery, activation, and integrity tests and public contracts. Do not weaken strict validation or expose bypass APIs. Do not implement LLM calls, prompt construction, system or user role messages, model APIs, agents, Hermes, MCP, authorization decisions, authentication, UI, new databases or object stores, automatic refresh, watchers, event streaming, full-text search, embeddings, vector databases, semantic ranking, relevance scoring, or knowledge graph persistence. Do not add a framework or dependency unless the current Node.js and TypeScript platform cannot satisfy the approved deterministic contracts; if a dependency is unavoidable, document alternatives, security and determinism impact, and the decision in `ARCHITECTURE_DECISIONS.md` before implementation. Use pure functions for ordering, selection, canonicalization, budget arithmetic, truncation, evidence generation, fingerprinting, and verification. Isolate durable registry and repository effects behind current application interfaces. Use accessor-safe raw-record validation and preserve physical path privacy. Use OS temporary directories or isolated roots for tests and never write generated context packages to the developer's real `.founderos/runtime/` directory. Never modify canonical `docs/` or `knowledge/` sources. Update only documentation reflecting implemented behavior, including the root README, documentation index, changelog, relevant package READMEs and exports, and architecture decisions. Add an ADR covering the governed context assembly boundary, active snapshot binding, deterministic selection policy, budget authority, truncation policy, evidence model, context identity, independent verification, and deferred LLM, agent, authorization, and semantic retrieval capabilities. Before completion run `pnpm format:check`, `pnpm lint`, `pnpm build`, `pnpm typecheck`, `pnpm test`, and `git diff --check`, plus any additional repository-defined gates. All prior milestone regressions and all new tests must pass. Inspect `git status` and ensure only intended Milestone 10 changes exist and no machine-local artifacts are staged. Perform a final independent whole-branch review focused on active-snapshot bypass, registry-integrity bypass, mutable aliasing, nondeterministic ordering, locale-sensitive behavior, hidden semantic ranking, silent required-object omission, incorrect budgets, Unicode-unsafe truncation, incomplete evidence, forged provenance acceptance, physical path leakage, wall-clock-dependent identity, fingerprint forgery, repository mismatch, and accidental LLM or agent coupling. Fix every Critical, Important, or Minor finding before declaring `GO`, otherwise report `NOT READY` with exact reasons. Prepare the result as commit-ready but do not merge into `main`. If the user has not explicitly authorized commit and push, leave changes uncommitted and report that state. If explicitly authorized, create a clean conventional commit, push `codex/milestone-10`, and prepare a Pull Request rather than merging locally. Return a final report titled `FounderOS Milestone 10 Completion Report` containing status `GO` or `NOT READY`; branch, base, worktree, commit, and push state; implementation summary; exact context assembly workflow; all added and modified files; tests added by category and final total tests; exact verification results; evidence of active snapshot and registry integrity binding; evidence of deterministic selection and candidate-order independence; evidence of exact object and character budget enforcement; evidence of required-object behavior; evidence of exclusion, omission, and truncation records; evidence of byte-identical reproducibility; evidence of independent fingerprint verification and tamper rejection; architecture impact and dependency direction; known limitations; deliberately deferred capabilities; final independent review findings; recommended next milestone; and Pull Request readiness. Milestone 10 may be reported as `GO` only when FounderOS can start from the durably verified active Knowledge Snapshot, execute the approved deterministic query, assemble an immutable and budget-bounded context package, preserve complete provenance and omission evidence, independently verify every binding and fingerprint, and reproduce byte-identical canonical output from the same governed inputs without invoking an LLM, agent, semantic ranking system, or model-specific tokenizer. Prioritize auditability, reproducibility, evidence completeness, and governance integrity over context size or apparent intelligence.
