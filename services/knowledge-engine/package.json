{
  "name": "@founderos/knowledge-engine",
  "version": "0.1.0",
  "private": true,
  "description": "KnowledgeOS ingestion, migration, repository, and query foundation",
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "migrate": "node dist/cli/migrate.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@founderos/knowledge-schema": "workspace:*",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
